struct a {
  int(3)  a0;
};

struct b {
  bool    b0;
  i3      b1;
  uint(4) b2;
  a       b3;
};

fsm atbits {
  in sync uint(@bits(b)) p_i;
  out sync u11 p_o;

  void main() {
    p_o.write(p_i.read());
    fence;
  }
}
